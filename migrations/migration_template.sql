-- migrate:up

-- Add SQL to be executed here, E.g.
CREATE TABLE open_app_builder.deployments(...)

-- migrate:down
-- Custom logic used for rollback. Currently not used (to avoid accidental data loss)